@page "/recurring-expenses"

@using ExpenseTracker.Core.Models
@using ExpenseTracker.UI.Services

@inject RecurringExpenseApiService RecurringExpenseService


<div class="recurring-expenses-page">

    <!-- ============================== -->
    <!-- HEADER -->
    <!-- ============================== -->

    <div class="recurring-header">

        <div>

            <h2 class="page-title">
                🔄 Recurring Expenses
            </h2>

            <p class="page-subtitle">
                Manage your monthly recurring payments
            </p>

        </div>

    </div>


    <!-- ============================== -->
    <!-- ERROR -->
    <!-- ============================== -->
    @if (!string.IsNullOrWhiteSpace(errorMessage))
    {
        <div class="alert alert-danger alert-dismissible fade show compact-alert"
             role="alert">

            <div>

                <strong>
                    ❌ Something went wrong.
                </strong>

                <div class="mt-1">
                    @errorMessage
                </div>

            </div>

            <button type="button"
                    class="btn-close"
                    aria-label="Close"
                    @onclick="ClearError">
            </button>

        </div>
    }


    @if (recurringExpenses == null)
    {
        <!-- ============================== -->
        <!-- LOADING -->
        <!-- ============================== -->

        <div class="loading-card">

            <div class="spinner-border text-primary"
                 role="status">
            </div>

            <span>
                Loading recurring expenses...
            </span>

        </div>
    }
    else
    {
        <!-- ============================== -->
        <!-- ADD / EDIT -->
        <!-- ============================== -->

        <div class="recurring-form-card">

            <div class="form-header">

                <h4>
                    @(editingExpenseId == null
                                    ? "Add Recurring Expense"
                                    : "Edit Recurring Expense")
                                                           </h4>

                <p>
                    @(editingExpenseId == null
                                    ? "Set up a payment that repeats every month."
                                    : "Update the selected recurring expense.")
            </p>

            </div>


            <!-- FORM -->
            <div class="recurring-form-grid">

                <!-- TITLE -->
                <div class="form-field">

                    <label>
                        Title *
                    </label>

                    <input class="compact-input"
                           placeholder="e.g. Internet"
                           @bind="newExpense.Title" />

                @if (showValidation &&
                                string.IsNullOrWhiteSpace(newExpense.Title))
                    {
                        <div class="field-error">
                            Title is required.
                        </div>
                    }

                </div>


                <!-- AMOUNT -->
                <div class="form-field">

                    <label>
                        Amount *
                    </label>

                    <input class="compact-input"
                           type="number"
                           step="0.01"
                           min="0.01"
                           placeholder="e.g. 35.00"
                           @bind="newExpense.Amount" />

                    @if (showValidation &&
                                    newExpense.Amount <= 0)
                    {
                        <div class="field-error">
                            Amount must be greater than 0.
                        </div>
                    }

                </div>


                <!-- DAY -->
                <div class="form-field">

                    <label>
                        Day of month *
                    </label>

                    <input class="compact-input"
                           type="number"
                           min="1"
                           max="31"
                           placeholder="1 - 31"
                           @bind="newExpense.DayOfMonth" />

                    @if (showValidation &&
                                    (newExpense.DayOfMonth < 1 ||
                                    newExpense.DayOfMonth > 31))
                    {
                        <div class="field-error">
                            Day must be between 1 and 31.
                        </div>
                    }

                </div>


                <!-- ACTIVE -->
                <div class="form-field active-field">

                    <label>
                        Status
                    </label>

                    <label class="active-toggle">

                        <input type="checkbox"
                               @bind="newExpense.IsActive" />

                        <span class="toggle-track">
                            <span class="toggle-knob"></span>
                        </span>

                        <span class="toggle-text">
                            Active
                        </span>

                    </label>

                </div>

            </div>


            <div class="form-help">
                Day of the month when this expense is due.
            </div>


            @if (showValidation && !IsRecurringExpenseValid)
            {
                <div class="validation-summary">
                    Please correct the highlighted fields before saving.
                </div>
            }


            <!-- BUTTONS -->
            <div class="form-actions">

                @if (editingExpenseId == null)
                {
                    <button type="button"
                            class="btn btn-primary compact-button"
                            @onclick="AddRecurringExpense">

                        ➕ Add Recurring Expense

                    </button>
                }
                else
                {
                    <button type="button"
                            class="btn btn-warning compact-button"
                            @onclick="UpdateRecurringExpense">

                        💾 Save Changes

                    </button>

                    <button type="button"
                            class="btn btn-light border compact-button"
                            @onclick="CancelEdit">

                        Cancel

                    </button>
                }

            </div>

        </div>


        <!-- ============================== -->
        <!-- LIST -->
        <!-- ============================== -->
        @if (!recurringExpenses.Any())
        {
            <div class="empty-recurring-card">

                <div class="empty-recurring-icon">
                    🔄
                </div>

                <h5>
                    No recurring expenses yet
                </h5>

                <p>
                    Add your first recurring expense above.
                </p>

            </div>
        }
        else
        {
            <div class="row g-3">

                @foreach (var expense in recurringExpenses)
                {
                    <div class="col-xl-4 col-lg-6">

                        <div class="recurring-card">

                            <!-- HEADER -->
                            <div class="recurring-card-header">

                                <div class="recurring-title-area">

                                    <div class="recurring-icon">
                                        🔄
                                    </div>

                                    <div class="recurring-title-content">

                                        <h5>
                                            @expense.Title
                                        </h5>

                                        <span>
                                            Every month · day @expense.DayOfMonth
                                        </span>

                                    </div>

                                </div>


                                @if (expense.IsActive)
                                {
                                    <span class="status-badge status-active">
                                        Active
                                    </span>
                                }
                                else
                                {
                                    <span class="status-badge status-inactive">
                                        Inactive
                                    </span>
                                }

                            </div>


                            <!-- AMOUNT -->
                            <div class="recurring-amount">

                                @expense.Amount.ToString("C")

                            </div>


                            <!-- DETAILS -->
                            <div class="recurring-info">

                                <div class="recurring-info-row">

                                    <span>
                                        📅 Schedule
                                    </span>

                                    <strong>
                                        Every month on day @expense.DayOfMonth
                                    </strong>

                                </div>


                                <div class="recurring-info-row">

                                    <span>
                                        🔄 Last generated
                                    </span>

                                    <strong>

                                        @if (expense.LastGeneratedDate.HasValue)
                                        {
                                            @expense.LastGeneratedDate.Value.ToShortDateString()
                                        }
                                        else
                                        {
                                            <span class="muted-value">
                                                Not yet
                                            </span>
                                        }

                                    </strong>

                                </div>

                            </div>


                            <!-- ACTIONS -->
                            <div class="recurring-actions">

                                @if (expense.IsActive)
                                {
                                    <button class="action-button action-generate"
                                            @onclick="() => GenerateExpense(expense.Id)">

                                        ⚡ Generate

                                    </button>
                                }


                            <button class="action-button action-edit"
                                    @onclick="() => EditRecurringExpense(expense)">

                                ✏️ Edit

                            </button>


                            <button class="action-button action-toggle"
                                    @onclick="() => ToggleActive(expense)">

                                @(expense.IsActive
                                                        ? "⏸ Disable"
                                                        : "▶ Enable")

                    </button>


                                <button class="action-button action-delete"
                                        @onclick="() => DeleteRecurringExpense(expense.Id)">

                                    🗑 Delete

                                </button>

                            </div>

                        </div>

                    </div>
                }

            </div>
        }
    }

</div>


@code {

    private List<RecurringExpense>? recurringExpenses;

    private int? editingExpenseId;

    private bool showValidation;

    private string? errorMessage;


    private RecurringExpense newExpense = new()
    {
        IsActive = true
    };


    // =========================
    // INITIALIZATION
    // =========================

    protected override async Task OnInitializedAsync()
    {
        try
        {
            ClearError();

            recurringExpenses =
                await RecurringExpenseService.GetAllAsync();
        }
        catch (Exception ex)
        {
            SetError(ex);

            recurringExpenses =
                new List<RecurringExpense>();
        }
    }


    // =========================
    // VALIDATION
    // =========================

    private bool IsRecurringExpenseValid =>
        !string.IsNullOrWhiteSpace(newExpense.Title)
        && newExpense.Amount > 0
        && newExpense.DayOfMonth >= 1
        && newExpense.DayOfMonth <= 31;


    // =========================
    // ADD
    // =========================

    private async Task AddRecurringExpense()
    {
        showValidation = true;

        if (!IsRecurringExpenseValid)
            return;


        try
        {
            ClearError();

            await RecurringExpenseService.AddAsync(
                newExpense);


            recurringExpenses =
                await RecurringExpenseService.GetAllAsync();


            showValidation = false;


            newExpense =
                new RecurringExpense
                {
                    IsActive = true
                };
        }
        catch (Exception ex)
        {
            SetError(ex);
        }
    }


    // =========================
    // EDIT
    // =========================

    private void EditRecurringExpense(
        RecurringExpense expense)
    {
        ClearError();

        editingExpenseId =
            expense.Id;

        showValidation =
            false;


        newExpense =
            new RecurringExpense
            {
                Id = expense.Id,

                Title = expense.Title,

                Amount = expense.Amount,

                DayOfMonth = expense.DayOfMonth,

                IsActive = expense.IsActive,

                CreatedAt = expense.CreatedAt,

                LastGeneratedDate =
                    expense.LastGeneratedDate
            };
    }


    // =========================
    // UPDATE
    // =========================

    private async Task UpdateRecurringExpense()
    {
        showValidation = true;

        if (!IsRecurringExpenseValid)
            return;


        try
        {
            ClearError();

            await RecurringExpenseService.UpdateAsync(
                newExpense);


            recurringExpenses =
                await RecurringExpenseService.GetAllAsync();


            CancelEdit();
        }
        catch (Exception ex)
        {
            SetError(ex);
        }
    }


    // =========================
    // CANCEL
    // =========================

    private void CancelEdit()
    {
        editingExpenseId =
            null;

        showValidation =
            false;


        newExpense =
            new RecurringExpense
            {
                IsActive = true
            };
    }


    // =========================
    // ENABLE / DISABLE
    // =========================

    private async Task ToggleActive(
        RecurringExpense expense)
    {
        try
        {
            ClearError();

            expense.IsActive =
                !expense.IsActive;


            await RecurringExpenseService.UpdateAsync(
                expense);


            recurringExpenses =
                await RecurringExpenseService.GetAllAsync();
        }
        catch (Exception ex)
        {
            SetError(ex);
        }
    }


    // =========================
    // GENERATE
    // =========================

    private async Task GenerateExpense(int id)
    {
        try
        {
            ClearError();

            await RecurringExpenseService.GenerateAsync(
                id);


            recurringExpenses =
                await RecurringExpenseService.GetAllAsync();
        }
        catch (Exception ex)
        {
            SetError(ex);
        }
    }


    // =========================
    // DELETE
    // =========================

    private async Task DeleteRecurringExpense(int id)
    {
        try
        {
            ClearError();

            await RecurringExpenseService.DeleteAsync(
                id);


            recurringExpenses =
                await RecurringExpenseService.GetAllAsync();


            if (editingExpenseId == id)
            {
                CancelEdit();
            }
        }
        catch (Exception ex)
        {
            SetError(ex);
        }
    }


    // =========================
    // ERROR HANDLING
    // =========================

    private void SetError(Exception ex)
    {
        errorMessage =
            string.IsNullOrWhiteSpace(ex.Message)
                ? "Unable to complete the operation. Please try again."
                : ex.Message;
    }


    private void ClearError()
    {
        errorMessage = null;
    }

}